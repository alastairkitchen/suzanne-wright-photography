import React from "React";
import Navigation from "./navigation";

const Header = props => (
  <header>
    <div className="site-header">
      <h1>Suzanne Wright</h1>
      <Navigation />
    </div>
  </header>
);

export default Header;
