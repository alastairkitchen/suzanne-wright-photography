import React from "react";
import SiteLayout from "../components/layout/siteLayout";

const Weddings = ({ data }) => {
  return (
    <SiteLayout>
      weddings
    </SiteLayout>
  );
};

export default Weddings;

